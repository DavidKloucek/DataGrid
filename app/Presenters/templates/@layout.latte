<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width">
	<title>{ifset title}{include title|stripHtml} | {/ifset}Datagrid example</title>
	<script src="{$baseUrl}/js/dist/vendor.bundle.js"></script>
	<script src="{$baseUrl}/js/dist/main.bundle.js"></script>
	{block head}{/block}
</head>

<body>

	<div n:foreach="$flashes as $flash" n:class="flash, $flash->type">{$flash->message}</div>

	{include content}

	{block scripts}
	<link rel="stylesheet" href="{$baseUrl}/css/bootstrap.min.css" crossorigin="anonymous">
	<link href="{$baseUrl}/css/style.css" rel="stylesheet">
	{/block}
</body>
</html>
